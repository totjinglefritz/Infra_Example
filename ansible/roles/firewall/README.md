# README for firewall role. 

## Purpose

This role installs and configures a client side `firewalld` service

## Configuration

Note that `./roles/firewall/vars/main.yml` expects the 
Hostnames and IP addresses in a python dictionary format.

